import { ContributionsEqualTotalCostDirective } from './contributions-equal-total-cost.directive';

describe('ContributionsEqualTotalCostDirective', () => {
  it('should create an instance', () => {
    const directive = new ContributionsEqualTotalCostDirective();
    expect(directive).toBeTruthy();
  });
});
